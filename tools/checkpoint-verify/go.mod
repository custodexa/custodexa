// 獨立模組：刻意不屬於 backend module，也不 require 任何外部套件。
// 稽核方複製本目錄即可 `go build`，無需取得產品原始碼。
module checkpoint-verify

// 下方 `go` 指令是**建置本工具所需的最低 Go 版本**，不是本專案使用的版本
// （產品側為 backend/go.mod 的 `go 1.26.0`、容器基底 golang:1.26.6-alpine）。
//
// **兩者刻意脫鉤，此處取低不取高。** 本工具的價值在於稽核方能以自己手上的工具鏈
// 獨立重建（見 docs/security/audit-checkpoint-offline-verification.md §7），而離線
// 稽核環境未必能下載新工具鏈——`go` 指令高於對方的工具鏈時，不是「用舊版建」而是
// 直接拒絕建置（Go 1.21 起該指令為硬性下限），在無網路的環境即無從補救。
// 把下限拉到與產品同版，等於為零收益犧牲本工具唯一的存在理由。
// 下限低不等於建議以 EOL 工具鏈建置：稽核方應自行選用仍受支援的版本，
// 本宣告只是不替他做這個決定。
//
// 上調時機：僅當原始碼真的用到更新的語言特性或標準庫 API。
// 2026-08-18 實測：`golang:1.21-alpine`（go1.21.13）容器內 `go vet ./...` 與
// `go build ./...` 皆通過，故本宣告是可滿足的真實下限，不是未更新的殘留。
go 1.21
