# agent-channel-ui Specification

## Purpose
agent 通道的呈現面：主體類型一眼可辨、多資產申請與逐項審核、會話歸屬欄、任務視角、tool call 帳本檢視、熔斷處置與 owner 自助頁，全部沿既有設計系統與三語。

## Requirements

### Requirement: 主體類型的一眼可辨

凡呈現主體（使用者管理、會話列表與詳情、申請單與審核中心、審計時間軸、告警、稽核主體目錄）的介面，SHALL 在主體名稱處標示其類型（人類／AI agent），SHALL NOT 只以名稱區分。標示 SHALL 以文字承載語義（不得僅以顏色或圖示區分），並 SHALL 於三語各有對應文案。

agent 主體 SHALL 於標示旁同時可得其負責人（owner）：列表以次行或同儲存格副標呈現，詳情以獨立欄位呈現。

標示的判定來源 SHALL 為後端回傳的主體類型欄位，SHALL NOT 由名稱樣式、前綴或角色推導。

#### Scenario: 會話列表可辨主體類型

- **WHEN** 稽核者開啟會話列表，其中一場由 agent 執行、一場由人類執行
- **THEN** 兩列的主體名稱處各帶類型標示，agent 那列另可見其負責人；不需展開或懸停即可分辨

#### Scenario: 標示不靠顏色

- **WHEN** 以灰階呈現或關閉色彩的環境檢視同一列表
- **THEN** 類型仍可自文字讀出

#### Scenario: 類型來自後端欄位

- **WHEN** 一個 agent 主體的名稱與某人類帳號命名風格相同
- **THEN** 其列仍標示為 AI agent（判定不看名稱）

### Requirement: agent 主體與 token 管理畫面

系統 SHALL 於使用者管理介面提供 agent 主體的建立與檢視：建立表單 SHALL 要求指定負責人，SHALL NOT 提供密碼、多重驗證與登入相關欄位，且 SHALL NOT 提供審核者、管理者、稽核者角色選項。列表 SHALL 可依主體類型篩選。

token 管理 SHALL 於單一層級的側邊面板內完成，SHALL NOT 以對話框疊加對話框的方式開啟。面板 SHALL 呈現該主體的全部 token：名稱、建立者、建立時間、到期時間、最近使用時間、狀態（有效／已到期／已停用／已撤銷），並提供建立與撤銷。

建立 token 的回應明文 SHALL 於面板內一次性揭露，並 SHALL 明示「離開後無法再次取得」；畫面 SHALL 提供複製動作與明確的「已保存」確認動作。明文 SHALL 持續呈現至使用者按下「已保存」或關閉面板為止：面板內的清單重新讀取、以及父層清單刷新後改以等值新物件傳入同一主體，SHALL NOT 使明文消失——明文只發一次，畫面把它收掉就等於那把鑰匙作廢。明文 SHALL NOT 出現在列表、SHALL NOT 於重新載入後再次呈現。

面板 SHALL 於首屏直接呈現該主體的鑰匙摘要（可用把數、最近到期的一把、已停用把數），SHALL NOT 要求逐張卡片捲動比對才能得知哪把還能用、哪把快到期。

鑰匙狀態的顏色 SHALL 對應處置語義：待處置用琥珀、終局狀態用中性色。熔斷已解除後，因跳閘而停用且只能重新發證的鑰匙 SHALL NOT 再標為待處置。

撤銷 SHALL 二次確認，確認文案 SHALL 載明撤銷會使該 token 建立中的會話立即結束。

當主體有未處置的熔斷事件時，建立 token 的動作 SHALL 停用並說明原因與解除途徑。

#### Scenario: 建立 agent 主體無登入欄位

- **WHEN** 管理者開啟建立 agent 主體表單
- **THEN** 表單要求負責人，且沒有密碼、多重驗證欄位與審核者／管理者／稽核者角色選項

#### Scenario: token 明文只揭露一次

- **WHEN** 管理者建立 token 後關閉面板再開啟
- **THEN** 列表只見 token 名稱與狀態，明文不再呈現；建立當下畫面已明示離開後無法再取得

#### Scenario: 明文在刷新清單與父層換物件後仍在

- **WHEN** 發證成功後面板重新讀取鑰匙清單，且父層刷新名下清單並以等值新物件傳入同一主體
- **THEN** 明文仍呈現於面板，直到使用者按下「已保存」或關閉面板

#### Scenario: 首屏答出哪把還能用

- **WHEN** 使用者開啟某主體的鑰匙面板
- **THEN** 首屏即見可用把數、最近到期的一把及其時間、已停用把數，不必捲到底逐張比對

#### Scenario: 撤銷確認說明後果

- **WHEN** 管理者對一把有效 token 點撤銷
- **THEN** 確認步驟載明該 token 建立中的會話會立即結束，確認後列狀態轉為已撤銷

#### Scenario: 熔斷未處置時不能發新 token

- **WHEN** 該主體有未處置的熔斷事件
- **THEN** 建立 token 動作停用，畫面說明原因並指向處置入口

#### Scenario: 面板不疊層

- **WHEN** 管理者自使用者列表開啟 token 面板
- **THEN** 面板為單一層級的側邊面板，其內的建立與撤銷在同一層完成

### Requirement: 熔斷處置畫面

系統 SHALL 提供熔斷處置畫面，呈現觸發熔斷的事件序列：每筆的時間、被引用的目標識別、端點類別，以及跳閘時停用的 token。畫面 SHALL 同時呈現主體目前是否仍有未處置事件；有未處置事件時，首屏 SHALL 一併呈現本次自動停用的時刻（含日期與時區），SHALL NOT 要求讀者以事件列時間推測。

事件列中的目標協議 SHALL 以與會話列表同源的人話呈現（種類詞加協議碼），SHALL NOT 只以大寫協議碼呈現。

事件分類彩標的琥珀 SHALL 只用於「屬於目前待處置那次跳閘」的計入門檻事件；主體沒有待處置事件時，以及早於本次待處置時刻的歷史事件，SHALL 以中性色呈現。

解除操作 SHALL 限負責人或管理者可見與可用，SHALL 要求填寫原因（必填），並 SHALL 於送出前載明兩件事實：解除不會自動恢復已停用的 token（須重新發證）、熔斷當下已換發的連線票證與既有連線已失效。

畫面文案 SHALL 描述事實，SHALL NOT 斷言觸發成因（不得寫成「該 agent 嘗試攻擊」一類判斷語）。

告警清單中的熔斷告警 SHALL 提供進入本畫面的入口。

處置畫面首屏 SHALL 答出處置者的五個問題：為什麼被停、停用期間的影響範圍、如何解除、解除之後要做什麼（含重新發證的直達入口）、以及從告警清單如何進到本畫面。這些答案 SHALL NOT 需要捲動或展開才讀得到。

#### Scenario: 待處置時首屏可見停用時刻

- **GIVEN** 主體有未處置的熔斷事件，契約回傳 `breaker_pending_at`
- **WHEN** 負責人開啟處置畫面
- **THEN** 首屏待處置狀態旁可見該時刻的格式化日期時間（含時區），與事件列的時間互相獨立

#### Scenario: 已解除後歷史事件不再琥珀

- **GIVEN** 主體的熔斷已解除（`breaker_pending_at` 為空），事件序列仍有計入門檻的歷史事件
- **WHEN** 稽核者開啟處置畫面
- **THEN** 所有事件分類彩標為中性色，頁首狀態為「目前沒有待處置」

#### Scenario: 事件協議以人話呈現

- **GIVEN** 事件引用的目標為 MySQL 資產
- **WHEN** 開啟處置畫面
- **THEN** 事件行呈現「資料庫（MYSQL）」一類種類詞加協議碼（三語各有對應），而非只有 `MYSQL`

#### Scenario: 首屏答完處置五問

- **WHEN** 負責人自告警進入處置畫面且主體仍有未處置事件
- **THEN** 不捲動即見停用原因、影響範圍、解除表單、解除後重新發證的入口與本頁的進入途徑

#### Scenario: 鑰匙狀態完整且終局不著待處置色

- **WHEN** 該主體同時有有效、已停用與已撤銷的鑰匙
- **THEN** 每一把都標出目前狀態，已停用者併呈原因與時間，終局狀態不使用待處置色

#### Scenario: 事件序列可見

- **WHEN** 負責人自熔斷告警進入處置畫面
- **THEN** 畫面列出時間窗內計入的事件（時間、目標識別、端點類別）與被停用的 token

#### Scenario: 解除必填原因並說明後果

- **WHEN** 負責人點解除但未填原因
- **THEN** 送出被擋並提示原因必填；填妥後送出前的說明載明須重新發證、既有連線已失效

#### Scenario: 非負責人非管理者看不到解除

- **WHEN** 其他使用者檢視同一畫面
- **THEN** 事件可讀，解除動作不呈現

#### Scenario: 文案不斷言成因

- **WHEN** 檢視畫面上的任一說明文字
- **THEN** 文字陳述發生了什麼與影響範圍，不宣稱動機或意圖

### Requirement: 會話的歸屬欄

會話列表 SHALL 帶任務欄，呈現該會話所屬申請單 id（agent 會話必有值），並 SHALL 可由此進入任務視角。會話列表 SHALL 可依主體類型篩選。

會話列表的操作者欄與代表人欄 SHALL 完整呈現主體帳號名與代表人（或「無代表人」的明示空值），SHALL NOT 在預設視窗寬度（1440px）以省略號截去可辨識的部分；欄寬不足時 SHALL 換行而非截斷。協議欄的種類詞 SHALL 落在單一彩標且不折行、不溢出欄寬，協議碼 SHALL 以次行文字呈現。

會話詳情 SHALL 於基本資訊區呈現：actor（主體與其類型）、代表人（輔助模式時呈現被代理的人類，自主模式明示無代表人）、任務 id、執行當時的負責人快照、使用的 token 名稱。上述值 SHALL 標明為執行當時的快照，SHALL NOT 以當前帳號關係回查改寫。

會話進行中授權被撤銷者，列表與詳情 SHALL 標示該事實與發生時間，SHALL NOT 只以結束原因隱含表達。該狀態在會話列表、會話詳情、任務詳情三處 SHALL 使用同一詞組（三語各一），SHALL NOT 在不同畫面用不同說法描述同一件事。

會話列表 SHALL 於 agent 會話該列呈現其工具呼叫總數與其中被擋下的次數，SHALL NOT 要求進入詳情再逐列數。計數讀取失敗時 SHALL 呈現為未知並提供該列的重試，SHALL NOT 以零或空白代替。

#### Scenario: 身分欄不截斷

- **GIVEN** 一場由 `demo-agent-35f3` 自主執行的會話
- **WHEN** 稽核者以 1440px 寬開啟會話列表歷史記錄
- **THEN** 操作者欄完整可讀 `demo-agent-35f3`、代表人欄完整可讀「無代表人」，兩欄皆無省略號

#### Scenario: 協議彩標不溢出

- **GIVEN** 一場 PostgreSQL 會話與一場 SSH 會話
- **WHEN** 開啟會話列表
- **THEN** 彩標分別為「資料庫」與「終端」且各佔一行不溢出欄寬，協議碼 `POSTGRES`／`SSH` 在彩標下方次行

#### Scenario: 撤權斷線三畫面同詞

- **GIVEN** 一場進行中被撤權而中斷的 agent 會話
- **WHEN** 分別開啟會話列表、該會話詳情、所屬任務詳情
- **THEN** 三處描述該狀態的詞組逐字相同（時間可另附），且切換 en-US／ja-JP 亦各自逐字相同

#### Scenario: 列表答出呼叫與被擋次數

- **WHEN** 稽核者檢視會話歷史列表
- **THEN** 每場 agent 會話該列即見呼叫總數與被擋次數；讀取失敗的列顯示未知並可就地重試

#### Scenario: 自會話跳任務

- **WHEN** 稽核者在會話列表點一場 agent 會話的任務欄
- **THEN** 進入該申請單的任務視角

#### Scenario: 詳情呈現代表關係

- **WHEN** 稽核者檢視一場輔助模式的 agent 會話詳情
- **THEN** 可見 actor 為該 agent、代表人為指定的人類、任務 id、執行當時的負責人與 token 名稱，且標明為當時快照

#### Scenario: 自主模式明示無代表人

- **WHEN** 檢視一場自主模式的 agent 會話詳情
- **THEN** 代表人欄以明示的空值呈現（無代表人），而非留白或省略欄位

#### Scenario: 撤權後仍活動可見

- **WHEN** 一場會話的授權在進行中被撤銷
- **THEN** 列表與詳情標示該事實與發生時間

### Requirement: 任務視角

系統 SHALL 提供以申請單 id 為軸的任務視角，作為稽核者檢視 agent 工作的主入口，涵蓋列表與詳情兩層。列表 SHALL 可依主體、負責人、時間窗與報告狀態篩選。

任務詳情 SHALL 於同一頁依下列順序呈現，SHALL NOT 要求稽核者跨頁拼湊：

1. **這是誰、代誰、做什麼**：agent 主體與類型標示、負責人、代表人（或明示無）、申請理由、申請時間。
2. **被准了什麼**：逐項的資產、帳號範圍、時長、決定者、決定時間、下修或撤銷的事實；核准註記對稽核者 SHALL 完整可讀，SHALL NOT 遮罩。
3. **實際做了什麼**：該任務下的全部會話（含進入會話詳情與錄影的出口）與 tool call 帳本入口。
4. **agent 自己說做了什麼**：任務報告。報告 SHALL 標示為 agent 自述且非授權證據；有多個版本時 SHALL 預設呈現最新版並 SHALL 可展開全部版本，各版 SHALL 標示版本序與提交時間，SHALL NOT 只呈現最新版而使舊版無從取得。

**缺報告 SHALL 以任務關閉時刻判定**：關閉當下無報告者 SHALL 呈現「關閉時缺報告」的事實與任務關閉時間，
SHALL NOT 以空白呈現，SHALL NOT 推測原因；關閉後於修訂窗內補交的版本 SHALL 以修訂呈現，SHALL NOT 抹除該事實。

任務詳情首屏 SHALL 以摘要答出五個問題：做了什麼、哪裡被擋、誰負責、有沒有交報告、申請與核准的差在哪。呼叫統計讀取失敗時 SHALL 呈現為未知並提供重試，SHALL NOT 停留在計算中的字樣。

專業術語 SHALL 退居第二層：第一層用非專業人士可讀的語句陳述，機器碼、欄位名與識別碼於展開或次要位置呈現。名稱欄位在後端未投影名稱時 SHALL 明示未提供，SHALL NOT 以識別碼充當名稱。

#### Scenario: 首屏答出申請與核准的差

- **WHEN** 稽核者開啟一個核准範圍被縮限的任務詳情
- **THEN** 不捲動即見被縮限的項數與總項數

#### Scenario: 一頁答完稽核五問

- **WHEN** 稽核者開啟一個已結束任務的詳情
- **THEN** 同一頁依序可見主體與代表關係、逐項核准範圍與決定者、會話與帳本入口、任務報告

#### Scenario: 核准註記對稽核者可讀

- **WHEN** 具稽核權限者檢視任務詳情
- **THEN** 申請理由與核准註記以原文呈現，未被遮罩

#### Scenario: 缺報告是事實不是空白

- **WHEN** 一個任務已關閉但 agent 於關閉當下未提交報告
- **THEN** 報告區呈現「關閉時缺報告」與任務關閉時間，不推測原因

#### Scenario: 關閉後補交不抹除缺報告的事實

- **WHEN** 上述任務的提交者其後於修訂窗內補交一版
- **THEN** 該版本可讀，且畫面仍標示「關閉時缺報告」

#### Scenario: 報告標示為自述

- **WHEN** 檢視任一任務報告
- **THEN** 報告旁標示其為 agent 自述、非授權證據；有多版時預設呈現最新版，展開後各版皆可讀並帶版本序與提交時間

#### Scenario: 行話在第二層

- **WHEN** 未受訓的管理職人員開啟任務詳情
- **THEN** 第一層文字以日常語句陳述誰代誰做了什麼、准了什麼；機器碼與欄位名在展開後才出現

### Requirement: tool call 帳本檢視

系統 SHALL 於任務詳情與會話詳情各提供 tool call 帳本：任務帳本涵蓋該任務全部呼叫（含未進入會話者），會話帳本只含該會話的呼叫。

每列 SHALL 呈現：序號、時間、工具名稱、判定（待定／允許／拒絕／熔斷／速率上限）、被拒時的原因碼與其三語說明、
**回傳雜湊與回傳節錄**、遮罩筆數、耗時、結果狀態。參數 SHALL 以原文呈現；命中機敏規則的片段以固定標記呈現，該列 SHALL 提供「調閱原文」動作（限具 `audit:view` 權限者、必填理由），動作經專屬端點於伺服器端逐筆入審計、留痕失敗即拒絕交付；能力句柄以指紋呈現並標示為指紋。本規則前寫入、參數未留存的列 SHALL 標示「此版本前的紀錄未留存參數」，SHALL NOT 顯示為遮罩標記。欄名 SHALL 為「參數」而非「遮罩後參數」。

判定為**待定**的列 SHALL 呈現為「已送出而結果未知」，SHALL NOT 被呈現為成功或失敗——該狀態表示指令已轉送而結果未回，
把它歸進任一端都會讓稽核者讀到一件沒有發生的事。

帳本 SHALL 明示其與錄影的關係：**錄影保留未遮罩的原始畫面，帳本保留回傳摘要與雜湊**。
帳本 SHALL NOT 被表述為「agent 實際收到的內容」——它存的是遮罩後回傳文字的雜湊與一段有長度上限的節錄，
節錄旁 SHALL 標示其為節錄而非全文，否則稽核者會把看到的一段當成執行者當時看到的一切。

遮罩筆數非零的列 SHALL 可辨識，SHALL NOT 需逐列展開才知道發生過遮罩；遮罩是已完成的事實，其標示 SHALL NOT 使用待處置色。

帳本 SHALL NOT 對判定結果宣稱成因；被拒列只呈現規則與原因碼。

#### Scenario: 會話帳本與錄影並陳

- **WHEN** 稽核者在會話詳情切換到帳本分頁
- **THEN** 該會話的呼叫依序呈現，畫面說明錄影保留原始畫面而帳本保留回傳摘要與雜湊

#### Scenario: 待定列呈現為結果未知

- **WHEN** 帳本中有一列判定為待定（指令已送出而結果未回）
- **THEN** 該列呈現為「已送出而結果未知」，未被呈現為成功或失敗

#### Scenario: 被拒列帶原因碼

- **WHEN** 帳本中有一列判定為拒絕
- **THEN** 該列呈現原因碼與其當前語言的說明文字

#### Scenario: 遮罩可辨識

- **WHEN** 某列的遮罩筆數為 2
- **THEN** 該列在不展開的情況下即可看出發生過遮罩與其筆數

#### Scenario: 任務帳本涵蓋無會話呼叫

- **WHEN** agent 在該任務內呼叫過查詢類工具但未建立會話
- **THEN** 這些呼叫出現在任務帳本，會話帳本不含它們

#### Scenario: 被阻斷列直接可讀指令

- **WHEN** 帳本中有一列 `run_command` 因阻斷規則被拒
- **THEN** 該列不展開即可讀到被擋的指令原文與規則原因碼

#### Scenario: 機敏參數需理由才揭露

- **WHEN** 稽核者對遮罩筆數非零的列按「調閱原文」並填理由
- **THEN** 原文於該列內呈現，審計新增一筆含稽核者、帳本列 id 與理由的調閱列；未填理由時動作不可送出

#### Scenario: 舊列標示未留存

- **WHEN** 檢視本規則前寫入的帳本列
- **THEN** 參數欄顯示「此版本前的紀錄未留存參數」，不顯示遮罩標記亦不提供調閱動作

### Requirement: agent 通道畫面的三語與設計系統一致性

本 change 新增的全部畫面文字 SHALL 經 i18n 取得，SHALL NOT 出現硬編碼字面文字；三語 key 集合 SHALL 一致且無空值。後端回傳的錯誤機器碼 SHALL 經既有錯誤文字解析單一入口轉為當前語言文字。

畫面樣式 SHALL 沿用既有設計 token（`--ot-*`）與元件慣例，SHALL NOT 引入新的品牌色或字型。

#### Scenario: 無裸 key 與硬編碼

- **WHEN** 以三語各開啟本 change 的每個新畫面
- **THEN** 全部文字為當前語言，無裸 key、無殘留的其他語言字面

#### Scenario: 錯誤碼經單一入口翻譯

- **WHEN** 建立 token 時後端回 `RULE_AGENT_BREAKER_PENDING`
- **THEN** 畫面顯示該碼對應的當前語言說明，而非原始機器碼

### Requirement: 首屏主文字權重

鑰匙面板（使用者頁的 token 抽屜）、審核中心的逐項審核展開列、owner 的「我的 agent」頁，三語各自的首屏（1440×900）主內容區中，以主要文字色（`--ot-text-primary`）呈現的字數 SHALL 多於以次級文字色（`--ot-text-secondary`）呈現的字數。名稱、數字、帳號、時長、日期、負責人等關鍵資訊 SHALL 以主要文字色呈現；每個區塊的說明句 SHALL 至多一句。

#### Scenario: 三畫面三語量測通過

- **GIVEN** dev 堆疊上有一個 agent 主體、一張含兩項且一項待審的申請單、一位 owner
- **WHEN** 以量測腳本對三畫面各以 zh-TW、en-US、ja-JP 量測 `firstScreenTierChars`
- **THEN** 九組結果皆為 `primary > secondary`，且無 `gray-text-dominant` 違規

### Requirement: 會話詳情的阻斷紀錄

會話詳情的指令分頁 SHALL 將該會話因阻斷規則被攔下的指令（來自阻斷告警列）併入指令時間序，每筆標示「已阻斷，未送往目標」與命中規則名稱，SHALL NOT 與已執行指令混淆為同一種列，亦 SHALL NOT 因此改變指令稽核只記錄已送出指令的語義。錄影回放跳轉 SHALL 可由該筆定位到阻斷標記的時刻。

#### Scenario: 阻斷列出現在指令分頁

- **GIVEN** 會話 118 有一筆阻斷告警（`ssh 10.0.0.9`，規則「Agent 橫向移動阻斷」）
- **WHEN** 稽核者開啟該會話詳情的指令分頁
- **THEN** 時間序中可見該筆，帶「已阻斷，未送往目標」標示與規則名，且與其他三條已執行指令可區分

#### Scenario: 無阻斷時不出現空殼

- **WHEN** 會話沒有任何阻斷告警
- **THEN** 指令分頁不顯示阻斷相關區塊或提示
